Marks a change in the conversation itself, not a turn in it.

```jsx
<SystemMessage icon="folder-open">Context switched to the Checkout project</SystemMessage>
```

Rules:
- Tertiary ink, centred, no surface — it must read as chrome, not as a turn.
- One short clause, stated objectively.
- Never for an error the user must act on; that is an `Alert`.
