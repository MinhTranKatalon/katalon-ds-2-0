Names a keyboard shortcut in prose, a menu item or a command palette.

```jsx
<Kbd>⌘</Kbd> + <Kbd>K</Kbd>
```

Rules:
- One cap per key; the `+` between them is plain text.
- Mono, 24px tall, 2px radius — the tightest radius in the system.
- Never the only documentation of an action.
