Separates groups when a gap alone is not enough.

```jsx
<Divider />
<Divider label="Advanced" />
<Divider vertical />
```

Rules:
- Always 1px. Depth in this system is a border plus a surface step, never a shadow.
- Prefer spacing over rules; reach for a divider only when the grouping is ambiguous.
